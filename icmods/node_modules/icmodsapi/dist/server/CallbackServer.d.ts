/// <reference types="node" />
import { Server as HttpServer } from "http";
import { Server as HttpsServer, ServerOptions as HttpsServerOptions } from "https";
import Server from "./Server.js";
declare type WebServer = HttpServer | HttpsServer;
declare type ServerOptions = HttpsServerOptions & {
    server?: WebServer;
    port?: number;
    /**
     * Default "/hooks"
     */
    path?: string;
};
declare class CallbackServer extends Server {
    private _server;
    private readonly _options;
    constructor(options?: ServerOptions);
    private request;
    listen(): void;
    close(): void;
}
export default CallbackServer;
