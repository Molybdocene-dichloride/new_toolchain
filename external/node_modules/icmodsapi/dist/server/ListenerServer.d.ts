import ICModsAPI from "../API.js";
import Server from "./Server.js";
declare const Sort: typeof ICModsAPI.Sort;
declare type Sort = ICModsAPI.Sort;
declare class ListenerServer extends Server {
    private sort;
    private interval;
    private timer;
    private lastTime;
    constructor(interval?: number, sort?: Sort);
    private check;
    listen(): Promise<void>;
    close(): void;
}
export default ListenerServer;
